# Keep pip tooling stable (prevents resolver/build surprises)
pip
setuptools
wheel

# Use the official PyTorch CPU wheel index
--extra-index-url https://download.pytorch.org/whl/cpu

# Core
streamlit

# Hugging Face stack (CPU-friendly pins)
transformers
datasets
accelerate
sentencepiece
protobuf

# PyTorch CPU (from the extra index above)
torch

# Utilities (pin to known-good wheels)
rapidfuzz
pandas
numpy
scikit-learn
matplotlib

# (Optional) pin these to avoid churn you saw in the log
rich
markdown-it-py
mdurl
pygments
